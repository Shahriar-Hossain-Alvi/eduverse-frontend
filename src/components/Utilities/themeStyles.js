// themeStyles.js
const themeStyles = {
    background: {
        dark: "bg-base-200 text-white",
        light: "bg-white border text-black"
    },
    button: {
        dark: "text-white btn-success",
        light: "btn-success text-white"
    },
};

// how to use ${themeStyles.background[theme]}

export default themeStyles;
